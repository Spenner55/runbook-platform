# Workflow Schema

Canonical schema package scaffold for workflow definitions shared across services.

## Current Scope
- Bootstrap JSON schema placeholder copied from the current contracts scaffold

## Next Steps
- Decide whether this package will publish TypeScript helpers, Python helpers, or both
- Consolidate any duplicated schema references into a single canonical export
