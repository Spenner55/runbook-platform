# Workflow Schema

Canonical schema package scaffold for workflow definitions shared across services.

## Current Scope

- Contains a workflow schema placeholder copied from the current contracts scaffold.
- Not yet published or consumed as the sole source of truth.

## Next Ownership Decision

Before broad usage, decide whether `packages/workflow-schema` or `packages/contracts` owns the canonical workflow schema and update docs/code to point to one source.
