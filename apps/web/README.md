# Web App

Vite + React + TypeScript frontend for the runbook platform.

## Current Scope
- Basic application shell
- Placeholder landing page
- Ready for routing, query state, and authenticated UI work

## Commands
- `npm run dev`
- `npm run build`
- `npm run lint`

## Environment
The frontend reads `VITE_API_BASE_URL` from the repo root `.env`.
