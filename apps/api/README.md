# API Service

Django + DRF service for runbook, workflow, policy, execution, and audit APIs.

## Current Scope
- Django project scaffold with environment-based settings
- Health endpoint at `/health/`
- Placeholder app namespaces under `apps/`

## Next Steps
- Add Django app configs, models, serializers, and viewsets
- Register API routes by domain area
- Add authentication, permissions, and organization scoping
